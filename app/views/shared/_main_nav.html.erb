<% path = current_user.present? ? home_path : "/" %>
<ul>
  <%= content_tag(:li, "#{link_to h(t('general.home')), path}") %>
  <%= content_tag(:li, "#{link_to h(t('lesson.create_lesson')), new_lesson_path}") if current_user.present? %>
</ul> 

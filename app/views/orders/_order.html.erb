<tr class="<%= cycle("even", "odd") -%>">
  <td><%= link_to order.id, order_path(order) %></td>
  <td><%= l(order.cart.purchased_at) %></td>
  <td><%= link_to_profilejj(order.user) %></td>
  <td><%= number_to_currency(order.cart.total_discounted_price) %></td>
</tr>

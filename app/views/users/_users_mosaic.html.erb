<% users = object %>
<% if (users.present?) %>
  <ul class="users-mosaic  clearfix">
    <% users.each do |member| %>
      <li><%= avatar_link_to(member, :size => :tiny) %></li>
    <% end %>
  </ul>
<% else %>
  <p>none found</p>
<% end %>

<div class="fh-form">
  <ul id="profile-info" class="section">
    <li class="header">
      <span></span>
      <h3>Reset <%= link_to_profile @user%>'s Password</h3>
      <h4>Clicking the link below will trigger an email to be sent to this user. If the user clicks on the link in the email, their password will be reset.</h4>
    </li>
    <li>
      <span class="unlabelled">
        <%= link_to "Reset Password", reset_password_user_path(@user),  :method => :post %>
      </span>
      <% if @current_user.is_an_admin? %>
        <p class="help">(this will send an email to <%= mail_to h(@user.email) %>)</p>
      <% end -%>
    </li>
  </ul>
</div>

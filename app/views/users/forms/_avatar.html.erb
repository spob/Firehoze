<% form_for(:user, :url => accounts_path, :html => { :method => :put, :multipart => true, :id => "avatar" }) do |f| %>
  <%= f.error_messages %>
  <fieldset>
    <legend>Upload an Avatar</legend>
    <p>
      <%= avatar_tag @user, :size => :medium %>
    </p>
    <div>
      <%= f.label :avatar %>
      <%= f.file_field :avatar %>
    </div>
  </fieldset>
  <p>
    <%= f.submit "Update", :disable_with => translate('general.disable_with') %>
  </p>
  <% if @user.avatar? %>
    <%= link_to('Delete current', destroy_avatar_accounts_path(@user),
                html_options = {:method => :post, :confirm => 'Are you sure?'} ) %>
  <% end %>
<% end %>

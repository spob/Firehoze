<% title "Edit SKU &quot;#{h(@sku.sku)}&quot;" -%>

<%= render :partial => 'form', :locals => { :heading => "Edit SKU #{h(@sku.sku)}", :subtext => "SKUs are items that users can purchase in the online store."} %>
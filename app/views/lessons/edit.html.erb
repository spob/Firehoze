<% title "Edit lesson" -%>

<%= render :partial => 'form', :locals => { :new_lesson => false, :heading => "Edit Lesson #{link_to(h(@lesson.title), lesson_path(@lesson))}", :subtext => "<h4>Update attributes for the lesson</h4>" } %>

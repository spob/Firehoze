<% title "Edit lesson" -%>

<%= render :partial => 'form', :locals => { :heading => "Edit Lesson #{link_to(h(@lesson.title), lesson_path(@lesson))}", :subtext => "Update attributes for the lesson" } %>

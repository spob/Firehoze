<% title "#{h @lesson.title}" -%>
<% @current = 'lessons' %>

<%= javascript 'swfobject' %>

<div id="lesson-show">

  <%= render :partial => "categories/v2/breadcrumbs", :object => @lesson.category, :locals => { :mode => "lessons" } %>

  <div id="title-container" class="span-12">
    <div class="span-9" id="title">
      <h3><%= h @lesson.title -%></h3>
    </div>
    <div class="span-3 last" id="rating-container">
      <div id="rating">
        <div id="stars">
          <%= lesson_rating_for(@lesson, current_user) -%>
        </div>
        <div id="stats">
          Average Rating:
          <span id="<%= dom_id(@lesson) -%>_average" class="average"><%= current_average(@lesson) -%></span>
          (<span id="<%= dom_id(@lesson) -%>_count"><%= vote_counts_phrase(@lesson) -%></span>)
        </div>
      </div>
    </div>
  </div>

  <div class="span-12" id="author">by <%= link_to_profile(@instructor) -%></div>


  <div id="lesson-container" class="span-12 append-bottom">

    <div id="actions">
<%#= render :partial => 'lessons/groups', :object => @lesson %>
      <div><%= render :partial => "lessons/actions", :object => @lesson -%></div>
    </div>

    <% # lesson detail -%>
    <div id="lesson-detail" class="span-6">
      <div id="lesson-details-tabs" class="tab-container lesson-details">
        <ul class="tabs">
          <li><a href="#synopsis">Summary</a></li>
          <li><a href="#detail">Details</a></li>
          <li><a href="#instructor">Instructor</a></li>
        </ul>
        <div class="panes">
          <div id="tab-lesson-summary">
            <div id="lesson-synopsis">
              <%= h(@lesson.synopsis) -%>
              <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.</p>
              <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.</p>
              <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.</p>

            </div>
          </div>
          <div id="tab-lesson-detail">
            <table class="lesson-details">
              <% # language -%>
              <tr>
                <td class="header">Language:</td>
                <td><%= h language_value(@lesson.language) -%></td>
              </tr>
              <tr>
                <td class="header">Audience:</td>
                <td><%= h audience_value(@lesson.audience) -%></td>
              </tr>

              <% # duration -%>
              <% if @lesson.finished_video_duration -%>
                <tr>
                  <td class="header">Duration:</td>
                  <td><%= ChronicDuration.output(@lesson.finished_video_duration/1000, :format => :long) -%></td>
                </tr>
              <% end -%>

              <% # released on -%>
              <tr>
                <td class="header">Released on:</td>
                <td><%= l @lesson.created_at -%></td>
              </tr>

              <% # status %>
              <% if current_user.try("is_admin?") or @instructor == current_user -%>
                <tr>
                  <td class="header">Status:</td>
                  <td>
                    <div id='show_lesson_status'>
                      <%= h translate("lesson.#{@lesson.status}") -%>
                      <%= image_tag("general/spinners/ajax-loader_small_arrows.gif") if @lesson.status == LESSON_STATUS_CONVERTING %>
                      <% if @lesson.status == LESSON_STATUS_PENDING or @lesson.status == LESSON_STATUS_CONVERTING %>
                        <%= periodically_call_remote(:update => 'show_lesson_status', :url => { :action => :show_lesson_status, :id => @lesson }, :frequency => 5 ) %>
                      <% end %>
                    </div>
                  </td>
                </tr>
              <% end -%>

              <% # groups %>
              <% @groups = @lesson.active_groups.public(:select => [:id, :name]) + @lesson.active_groups.private(:select => [:id, :name]) %>
              <% unless @groups.empty? %>
                <tr>
                  <td class="header">Groups:</td>
                  <td>
                    <% @groups.each do |group| %>
                      <%= link_to h(group.name), group_path(group) %>&nbsp;
                    <% end %>
                  </td>
                </tr>
              <% end %>

              <% # students %>
              <tr>
                <td class="header">Students:</td>
                <td><%= number_of_students_phrase(@lesson) -%></td>
              </tr>

              <% # free credits %>
              <% if @lesson.has_free_credits?  %>
                <tr>
                  <td class="header">Free lessons</td>
                  <td><%= free_remaining_text(@lesson) %></td>
                </tr>
              <% end %>

              <% # tags %>
              <tr>
                <td class="header">Tags:</td>
                <td>
                  <% @lesson.tag_counts.each do |tag| %>
                    <%= link_to h(tag.name), tagged_with_lessons_path(:collection => :tagged_with, :tag => tag.name) %>
                  <% end %>
                </td>
              </tr>
            </table>
          </div>
          <div id="tab-lesson-instructor">
            <div id="avatar-container"><%= avatar_link_to(@instructor, :size => :smaller) %></div>
            <div id="bio">
              <%= link_to_profile(@instructor) %><br />
              <div id="lesson-instructor-bio">
                <%= @instructor.bio %>
              </div>

              <% # LESSON BY THIS INSTRUCTOR -%>
              <% if @instructor.instructed_lessons.size > 1 %>
                <div id="instructor_accordion_container" class="append-bottom">
                  <div id="instructor_accordion">
                    <h3><a href="#">Lessons by <%= truncate(h(@instructor.login), :length => 27) %></a></h3>

                    <div class="content">
                      <%= render :partial => "lessons/lesson_brief", :collection => (@instructor.instructed_lessons.reject{ |lesson| lesson == @lesson }) %>
                    </div>
                  </div>
                </div>
              <% end %>


            </div>
          </div>
        </div><!-- end panes -->
      </div><!-- end lesson-details-tabs -->
    </div><!-- end lesson-detail -->

    <% # lesson player -%>
    <div id="lesson-player" class="span-6 last">
      <div><%= render :partial => 'lessons/player', :object => @lesson -%></div>
    </div>
  </div>


  <div>
    <div class="span-12">
      <% # LESSON TABS -%>
      <div id="lesson-content-tabs" class="tab-container"   style="border: solid 0px lime">
        <ul class="tabs">
          <%= content_tag(:li, "#{ link_to h(t('lesson.notes')), lesson_notes_lesson_path(@lesson, :style => 'tab') }")  unless @lesson.notes.blank? and @lesson.attachments.empty? %>
          <li><%= link_to h(t('lesson.reviews')), lesson_reviews_path(@lesson, :style => 'tab', :per_page => REVIEWS_ON_LESSON_SHOW_PER_PAGE) %></li>
          <li><%= link_to h(t('lesson.discussion')), lesson_lesson_comments_path(@lesson, :style => 'tab', :per_page => COMMENTS_ON_LESSON_SHOW_PER_PAGE) %></li>
          <li><%= link_to "Recommendations", recommend_lesson_path(@lesson, :style => 'tab') %></li>
          <li><%= link_to "Stats", stats_lesson_path(@lesson, :style => 'tab') %></li>
          <li><%= link_to "Groups", stats_lesson_path(@lesson, :style => 'tab') %></li>
        </ul>

        <!-- single pane (ajax'd)-->
        <div class="panes">
          <div style="display:block"></div>
        </div>
      </div>
    </div>
  </div>
</div>

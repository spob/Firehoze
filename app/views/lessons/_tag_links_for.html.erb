<% lesson = object %>

<% unless lesson.tag_counts.empty? %>
  <b>Tags:</b>
  <% lesson.tag_counts.each do |tag| %>
    <%= link_to tag.name, list_lessons_path(:collection => :tagged_with, :tag => tag.name) %>
  <% end %>
<% end %>

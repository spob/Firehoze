<% lesson = object %>

<div class="span-6 lesson_left">
  <div><%= img_tag_lesson_tn(lesson) %></div>
  <div><%= ratings_for(lesson, :static) %></div>
  <%= content_tag(:div, free_remaining_text(lesson)) if lesson.has_free_credits? %>
  <div><%= number_of_students_phrase(lesson) %></div>
  <div>Posted: <%=  time_ago_in_words(lesson.created_at) %> ago</div>
  <div><%= render :partial => "lessons/stats", :object => lesson %></div>
</div>

<div class="span-26 lesson_center">
  <div><%= link_to lesson.title, lesson_path(lesson), :class => :lesson_title_link %> <%= content_tag(:span, "(#{owned_it_phase(lesson)})" ) unless owned_it_phase(lesson).blank? %></div>
  <div><%= lesson.synopsis %></div>
  <div class="clearfix">
    <%= render :partial => "lessons/tag_links_for", :object => lesson, :locals => { :css_classes =>  "span-20 lesson_center" } %>
    <div class="lesson_center last" style="text-align:right; white-space:nowrap"><!-- reserved for later --> </div>
  </div>
  <div><%= render :partial => "lessons/actions", :object => lesson %></div>
</div>

<div class="span-6 last lesson_right">
  <div><%= link_to_profile(lesson.instructor) %></div>
  <div>user avatar</div>
  <div>Member Since:<br /><%= lesson.instructor.created_at.to_date.to_s(:long) %></div>
</div>
<!-- <div class="clear span-16 prepend-6 append-6 last lesson_footer">some base content</div> -->
<hr />

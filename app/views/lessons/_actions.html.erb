<%
lesson = object
%>

<%= button_to_buy_or_watch(lesson) %>
<%= button_to_preview(lesson) %>
<%= button_to_wish(lesson) %>
<%= button_to "Edit", edit_lesson_path(lesson), :method => :get if lesson.instructed_by?(current_user) or current_user.try("is_admin?") %>

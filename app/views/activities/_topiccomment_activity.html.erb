<b><%=link_to_profile(activity.actor_user) %></b> added the comment "<b><%= truncate(h(activity.trackable.body), :length => 50) %></b>"
to the discussion topic
<b><%= link_to(h(activity.activity_object_human_identifier), topic_path(activity.activity_object_id)) %></b>
within the group
<b><%= link_to(h(activity.secondary_activity_object_human_identifier), group_path(activity.secondary_activity_object_id)) %></b>
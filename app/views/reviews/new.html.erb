New Review for Lesson <%= @lesson.title %>

<% form_for :review, :url => lesson_reviews_url(@lesson) do |f| %>
    <%= f.error_messages %>
    <p>
      <%= f.label 'Headline' %><br/>
      <%= f.text_field :title %>
    </p>
    <p>
      <%= f.label :body %> <br/>
      <%= f.text_area :body %>
    </p>
    <p><%= f.submit "Submit", :disable_with => translate('general.disable_with') %></p>
<% end %>


<p><%= link_to "Cancel", lesson_reviews_path(@lesson) %></p>

<div id="balance">
  Credits available: <%= user.available_credits.count %>
  <% if user.verified_instructor? %>
    | Earnings Owed: <%= link_to_if user.unpaid_credits_amount > 0, number_to_currency(user.unpaid_credits_amount), show_unpaid_payment_path(user) %>
  <% end %>
</div>

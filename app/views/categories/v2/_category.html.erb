<div class="category-container">
  <h3><%= link_to category.name, category_path(category) %></h3>
  <%= content_tag(:p, truncate(child_categories_names(category), :length => 40)) if category.has_children? %>
</div>

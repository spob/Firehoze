<% title "Browse by Categories" -%>

<h3>Browse by Categories</h3>

<% for category in @categories %>
    <%= link_to category.name, category_path(category) %>
    <br/><br/>
<% end %>

I'm just putting activities here because I need a place to put it...it should go on my firehoze
<br/>

<h3>Activity</h3>
View Activity:
<%= link_to_unless session[:browse_activities_by] == 'ALL', 'by everybody', categories_path %> |
<%= link_to_unless session[:browse_activities_by] == 'BY_ME', 'by me', categories_path(:browse_activities_by => 'BY_ME') %> |
<%= link_to_unless session[:browse_activities_by] == 'ON_ME', 'on me', categories_path(:browse_activities_by => 'ON_ME') %>
<table>
  <tr>
    <th colspan="2">
      Recent Activity
    </th>
  </tr>
  <%= render @activities %>
</table>
<%= will_paginate @activities %>